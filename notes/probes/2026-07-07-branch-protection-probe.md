# Branch-protection probe — 2026-07-07

Throwaway PR created by the automated hardening session immediately after
flipping `enforce_admins=true` + `require_code_owner_reviews=true` (#100/#133),
to empirically verify whether the single-dev self-merge path is now blocked.
The PR is closed unmerged either way; the outcome is recorded on #100/#133.
